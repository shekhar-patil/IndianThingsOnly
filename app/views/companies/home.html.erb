<center/><br/>
<div class="home-page">
  <h1>Is it Indian Company?</h1>
  <br/>
  <div class='form-group'>
    <%= form_for Company.new, url: { action: :search_company }, method: 'post' do |form| %>
      <%= form.select :name, [], {}, {class: "select2", placeholder: 'Search company', data: { url: '/company_provider' }} %>
      <br/>
      <div class="actions">
        <%= form.submit 'Search', class: 'btn btn-info btn-sm'%>
      </div>
    <% end %>

    <br/><br/>
  </div>

  <h2 class="love-tag">
    Made with <i class="fa fa-heart" style="font-size:30px;color:red"></i> in India
  </h2>

  <br/><br/>

  <h4>
    Start using Indian products. Ban all chinese and foreign products.
  </h4>

  <br/>
  <h1>"आत्मनिर्भर भारत!!"</h1>

  <br/><br/><br/>

<h2><i>Most trending lists in India</i></h2>
<!-- Flickity HTML init -->
<div class='carousel-box'>
  <div class="carousel js-flickity" data-flickity-options='{ "wrapAround": true, "pageDots": false, "autoPlay": 2000 }'>
    <% @lists.each do |list| %>
        <div class="carousel-cell">
          <img src="<%= list.picture_url %>" alt="orange tree" />
          <div class="carousel-caption">
            <h2><%= link_to list.title, list_path(list)%></h2>
          </div>
        </div>

    <% end %>
  </div>
</div>
<br/><br/>

  <div>
    <div>
      <%= button_to "All Companies", companies_url, method: :get, class: 'btn btn-outline-success' %>
    </div>
    <br/>
    <div>
      <%= button_to "Suggest Indian Company", new_company_url, method: :get, class: 'btn btn-outline-success' %>
    </div>


